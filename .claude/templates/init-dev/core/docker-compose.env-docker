# Project Configuration
PROJECT_NAME={{PROJECT_NAME}}

# Container Naming (for multi-branch development)
# Example: my-app-branch1-app-1, my-app-main-app-1
APP_CONTAINER={{PROJECT_NAME}}-app-1
LINTER_CONTAINER={{PROJECT_NAME}}-linter-1

# Traefik Ports (modify if conflicts on dev machine)
EXTERNAL_HTTP_PORT=8000
EXTERNAL_HTTPS_PORT=4430
TRAEFIK_DASHBOARD_PORT=8080

# Alternative for dev machines with port conflicts:
# EXTERNAL_HTTP_PORT=8000
# EXTERNAL_HTTPS_PORT=8443
# TRAEFIK_DASHBOARD_PORT=8081

# Application Environment (Framework-Specific - Update as needed)
# These variables are injected to container BEFORE dotenv cascade
APP_ENV=development
NODE_ENV=development  
DJANGO_SETTINGS_MODULE=myapp.settings

# Framework-specific usage:
# Symfony: APP_ENV=dev → loads .env.dev cascade
# Laravel: APP_ENV=local → loads .env.local cascade  
# Django: DJANGO_SETTINGS_MODULE=myapp.settings.dev → direct settings
# Node.js: NODE_ENV=development → standard Node.js environment

# @AI-Kitchen: CONDITIONAL - Include based on selected database
# @AI-Kitchen: SUBSTITUTE - Replace {{PROJECT_NAME}} with real project name

# PostgreSQL Configuration (if using PostgreSQL)
# POSTGRES_DB={{PROJECT_NAME}}_dev
# POSTGRES_USER=dev  
# POSTGRES_PASSWORD=secret

# MySQL Configuration (if using MySQL)  
# MYSQL_DATABASE={{PROJECT_NAME}}
# MYSQL_USER={{PROJECT_NAME}}
# MYSQL_PASSWORD={{PROJECT_NAME}}
# MYSQL_ROOT_PASSWORD={{PROJECT_NAME}}

# MariaDB Configuration (if using MariaDB)
# MARIADB_DATABASE={{PROJECT_NAME}}
# MARIADB_USER={{PROJECT_NAME}}
# MARIADB_PASSWORD={{PROJECT_NAME}}
# MARIADB_ROOT_PASSWORD={{PROJECT_NAME}}

# @AI-Kitchen: CONDITIONAL - Include if Redis detected in stack
# Redis Configuration (if using Redis)
# (Redis typically needs no additional config)