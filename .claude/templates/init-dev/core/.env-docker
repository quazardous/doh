# Development Environment Configuration
# 
# ⚠️  DO NOT put APP_ENV/NODE_ENV/DJANGO_SETTINGS_MODULE here!
# These framework environment variables are injected at container level
# via docker-compose.yml BEFORE dotenv cascade to avoid circular dependencies
#
APP_NAME={{PROJECT_NAME}}
API_URL=http://localhost:8000
# APP_ENV=development  # ❌ WRONG! Framework env injected at container level

# Database
DATABASE_URL=postgresql://dev_user:dev_password@postgres:5432/{{PROJECT_NAME}}_dev

# Frontend Environment Variables (Exposed to Browser)
# These will be prefixed automatically by build tools:
# VITE_API_URL, REACT_APP_API_URL, VUE_APP_API_URL, NEXT_PUBLIC_API_URL
FRONTEND_API_URL=${API_URL}

# Mail Configuration
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_FROM=${APP_NAME}@localhost

# Cache
REDIS_URL=redis://redis:6379/0

# Session
SESSION_SECRET=dev-session-secret-change-in-production

# File Storage
STORAGE_TYPE=local
STORAGE_PATH=./var/storage

# Logging
LOG_LEVEL=debug
LOG_FILE=./var/log/app.log

# DOH Hello World validation secret (auto-generated per environment)
DOH_HELLOWORLD={{DOH_HELLOWORLD}}