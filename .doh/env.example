# DOH will use .doh/env to read DOH_* env
# see .claude/scripts/doh/lib/dohenv.sh
# IMPORTANT: exiting env var are preserved

# global states dir
# DOH_GLOBAL_DIR=~/.doh

# Version management
# DOH_TAG_BUMP=false  # Create git tags during version bumps (default: false)